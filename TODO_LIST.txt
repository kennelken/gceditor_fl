- fix renderbox overflow errors
- set colors for classes, tables, columns, rows (use as row color in color column)
- Navigation history and navigate back and forth alt+🡺, alt+🢀
- new column property: value checkers - a list of checkers (IChecker.validate(field, value)). Every checker should define its own unique properties.
---- Example of such checkers IntChecker, AllowedValuesChecker, BannedValuesChecker, DateChecker, ReferenceChecker, NotNullChecker.
---- Move "Is Unique" property there.
- new column type: formula. Allows to define a formula, that uses variables from a dictionary-like cell content.
---- Example:
-------- Formula: {x} + {y}
-------- Cell: {"x": "10", "y":"SomeTableRowId.SomeColumnName"}
- replace MenuBar with PlatformMenuBar when there are implementations for windows and linux
- remore false positive changes when it converts 0->0.0->0 in the model.json file



- in output json convert 'k'/'v' lists into maps
- check if non-trivial keys such as managed objects and
- save date and time as integers, remove parsing by regex
- save vectors/rects as string with ';'
- reimplement c# parsing to rely on built-in Json parsing mostly replace reference values with Ref<T> (consider implementing custom equals and hashcode to delegate it to an actual instance. Probably not possible when added as keys to dictionaries. Consider wrapping all objects to Ref<> and using id as a hascode).
- only add final lists in the end of normal json parsing